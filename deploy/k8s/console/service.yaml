apiVersion: v1
kind: Service
metadata:
  name: aiops-console
  labels:
    app.kubernetes.io/name: aiops-console
    app.kubernetes.io/part-of: aiops-sre-agent
spec:
  selector:
    app.kubernetes.io/name: aiops-console
  ports:
    - name: http
      port: 8080
      targetPort: http
