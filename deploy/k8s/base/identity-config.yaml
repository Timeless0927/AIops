apiVersion: v1
kind: ConfigMap
metadata:
  name: aiops-identity-config
  namespace: aiops
  labels:
    app.kubernetes.io/name: aiops-identity-config
    app.kubernetes.io/part-of: aiops-sre-agent
data:
  identity.yaml: |
    identity:
      store_path: "/data/aiops/gateway.db"
      ldap:
        enabled: false
